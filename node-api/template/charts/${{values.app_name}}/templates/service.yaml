apiVersion: v1
kind: Service
metadata:
  name: {{ include "backstage-node-app.fullname" . }}
  labels:
    {{- include "backstage-node-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backstage-node-app.selectorLabels" . | nindent 4 }}
